//! STUB — whole-project docs validation (SPEC §5.6).
use anyhow::Result;
pub fn run(_json: bool, _strict: bool, _deep: bool, _limit: Option<usize>, _threshold: Option<f64>) -> Result<i32> {
    eprintln!("not implemented yet");
    Ok(1)
}
