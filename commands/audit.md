---
description: Detector report for files
argument-hint: "[path]"
allowed-tools: Bash(mari *), Read, Edit
---

Run `mari audit $ARGUMENTS` for the human-facing detector report grouped by family with bad→good example fixes. Report only; never edit files from this command. Summarize the worst findings and offer the matching editorial verb (`/deslop`, `/tighten`, or `/clarify`) as the fix path.
